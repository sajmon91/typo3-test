# Add configuration for the editor for any configuration
# For complete documentation see http://docs.ckeditor.com/#!/api/CKEDITOR.config
editor:
  config:
    # the CSS file to be used inside the editor
    contentsCss: "EXT:intbuilder/Resources/Public/Frontend/dist/css/rte.min.css"

    # will be overridden by the record (if a language is set)
    defaultContentLanguage: "en"
    defaultLanguage: "en"
    height: 300
    width: "auto"
    toolbar: "Basic"
    uiColor: "#F8F8F8"
    removeDialogTabs: "image:advanced;link:advanced"

    # Ensure that special characters are not converted to HTML entities
    entities_latin: false
    entities: false

    removePlugins:
      # The image plugin doesn't support FAL
      - image
